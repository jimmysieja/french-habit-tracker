# --- secrets: never commit ---
.env
.env.local
# Google service-account key(s). This repo has no other .json at its root;
# if you add one later, un-ignore it explicitly with:  !myfile.json
*.json

# --- generated output ---
# `python build.py` / CI writes public/ and deploys it to Pages; not committed.
# (assets/*.svg ARE committed on purpose - the README embeds them.)
dashboard.html
*.local.html
/public/

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# --- os / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
